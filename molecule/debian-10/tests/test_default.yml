---
file:
  /usr/bin/wget:
    # required attributes
    exists: true
  /usr/bin/curl:
    # required attributes
    exists: true
  /usr/bin/jq:
    # required attributes
    exists: true
  /bin/netstat:
    # required attributes
    exists: true